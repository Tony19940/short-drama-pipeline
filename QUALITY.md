# 出片质量与本地观众

在 `CULTURE.md` 之外的硬规则。违反即重做，不「先过再改」。  
操作勾选见各集 `01-bible/CULTURE-CHECK.md`。

## 给谁看

默认 **本地轨**：金边/柬埔寨手机竖屏。中国观众是导出轨，不是主轨。两轨只换声和字幕，不换脸、不换景。

| | 本地轨 | 导出中国轨 |
|---|---|---|
| 画面 | 同一套 | 同一套 |
| 旁白/字幕 | 高棉语为主 | 中文旁白 + 中文字幕 |
| 人名 | Sophea、Vireak，不译成苏菲亚 | 可加中文注 |
| 梗 | 厂里传闻、婚礼棚丢脸、住女方家 | 不要改成「顾家家宴」 |

## 硬规则（全部强制）

**R1 本地轨才是主轨。** 先做给柬埔寨人看的那一版。中文是同一套画面的导出音轨，不许为了中国观众改情节、改地名、改羞辱方式。

**R2 人名不译。** 本地字幕和旁白用 Sophea、Vireak、Koh Pich。禁止「苏菲亚」「维克」「钻石岛顾家」。

**R3 钱和软件用金边的。** 情节只许走美元+瑞尔、ABA/Wing、Facebook/Telegram、Cellcard/Smart。禁止用人民币、支付宝、微信红包推进剧情。

**R4 一集 1–2 个真地点。** 必须能在柬埔寨指出来（厂门口、家里小店婚礼席、独立纪念碑）。禁止一集里从「乡下」瞬移到无名海边别墅。本集地点写在 `blueprint.md`，禁止中途改成宴会厅。

**R5 羞辱必须本地人能感到疼。** 厂服、口音、婚礼棚丢脸、住谁家成立。「你配不上顾家」作废。

**R6 佛、僧、王室、吴哥不许当搞笑或魔法道具。** 可以出镜，必须庄重。

**R7 华柬家庭可以是冲突方，禁止写成唯一的「中资反派脸谱」。**

**R8 招牌能看清就必须是高棉文为主。** 生成画面不准出现旁白字、标题、乱码中文。字幕后期叠。

**R9 双人必须先有身高图**（`scale.jpg`）再拆单人主图。脚同一地平线。禁止一集里男主突然变矮。详见 `productions/khmer-stories/CONSISTENCY.md`。

**R10 邻镜先对机位。** 同场必须 `cut=continue`。硬切只换场。先有该场 `blocking.jpg`。出视频前跑 `scripts/check_prod.py`。主路径永远是锁定首帧 FL2VA。同机位且人物有交集才吃 `{from}-last.jpg`；换机位用这一镜锁定首帧。动作要收住写设计 `end_frame`，禁止拿抽出的 `-last.jpg` 当尾帧。身份参考挂在首帧外面。

**R11 成片抽 3 个近景对 `face.jpg`。** 发际线、耳环、sbai 织纹对不上，只重跑那一镜。禁止为了救一镜新开一张脸。

**R12 BGM 用高棉流行/婚礼锣鼓/pinpeat，不要默认二胡土嗨或中国古风。** 不要角色口型配音。H3 关掉对白不会更便宜；要省钱走 Hailuo Fast 无声再 ffmpeg 贴轨。

## 跨集画面锁（配合 R9–R11）

模型没有记忆。一致性靠资产。

1. 每个角色、每个主场景全剧只许一次全新生图，得到 `master`。换装、日夜、湿身全部 `image_edit`。
2. 出视频时：首帧 + 该镜出场角色的 `face.jpg` / `master.jpg` 必须作为参考图。缺文件则 `render_shots.py` 拒绝提交。
3. `shots.json` 每镜必须有 `characters` 和 `tier`（`fast` 或 `h3`）。`h3` 每集不超过 3 镜。

## 镜头质量

1. 一镜一个动作或一次运镜。地点和人数不要同时跳。
2. 成人角色必须明确成年。短剧女主不是「幼态网红脸」。
3. 画面默认无对白口型。狠话用字幕：本地轨高棉文，导出轨中文。
4. H3 即使生成了人声，也不当配音轨。环境声 + BGM 后期叠。

## 关卡勾选

### Gate A 锁剧本（R1–R8）

见 `CULTURE.md` 第四节 + 本集 `01-bible/CULTURE-CHECK.md`。

### Gate B 锁资产（R9）

- [ ] 每个出场角色有 `master.jpg` + `face.jpg`
- [ ] 新首帧前每个出场角色有 `sheet.jpg`（旧海报可留对照；Gate B 仍可用旧主图锁定以便审分镜）
- [ ] 本集有双人戏则有 `scale.jpg`，相对身高写在角色卡上
- [ ] 每个主场景有空镜 `master.jpg`

### Gate S / C 锁舞台和分镜

- [ ] `confirm.md` 画风/画幅/旁白方案已锁
- [ ] `sets.json` + 每场 `blocking.jpg`
- [ ] `coverage.md` 一场有 master 和更紧的机位
- [ ] `beats.md` 新信息不重复；都市剧 8 秒内有冲突
- [ ] `shots.json` 过 `check_prod.py`（含 setup / move / derived_from / line / line_kind / speaker / lens / axis / camera / action / start / look / video_prompt）
- [ ] 没有一人一张全身摆拍

### Gate D 锁首帧（R8、R10）

- [ ] 每镜有第 0 秒 `start`，静帧只画这个起点
- [ ] 按镜顺序锁：上一镜正式首帧没锁，下一镜不能锁
- [ ] 邻镜能看出从上一张改，或已用上一镜末帧
- [ ] 招牌可辨认则为高棉文为主
- [ ] 没有生成字幕叠在画上

### Gate E 出视频

- [ ] `characters` 参考图齐全，并作为 Ref2VA 外挂，不替代首帧
- [ ] 开场/硬切/换机位用锁定首帧；同机位续才吃 `{from}-last.jpg`
- [ ] 若填了 `end_frame`，文件存在且不是 `-last.jpg`
- [ ] `tier=h3` ≤ 3
- [ ] 无声 Fast 为默认；H3 只打近景脸 / 仪式 / 必须卡环境声的动作
- [ ] 都市短剧：无声切只审脸。审剧情必须再跑 `mix_review_track.py`（工作旁白 + 字幕）

### Gate F 成片（R1、R2、R11、R12）

- [ ] 本地轨：高棉语配音（无声画面路径，`DUBBING.md`）。云端出片的人声/环境声不当成品
- [ ] 人名未译成中文音译（本地轨）
- [ ] BGM 是高棉流行或婚礼锣鼓，不是二胡古风
- [ ] 每个主角抽 3 个近景对过 `face.jpg`
- [ ] 导出中国轨只换声和字幕，画面与本地轨相同
