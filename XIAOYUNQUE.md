# 本地工程如何模仿小云雀

小云雀短剧 Agent 的单位是**一场戏**，不是一张图。  
本仓库用人审卡点复刻同一条链。禁止再走「静帧 → 各自动 6 秒 → concat」。

## 他们怎么走

1. 剧本进系统（或一句话生成）。
2. **先锁画风、画幅、旁白方案**（旁白改编也在这一步）。
3. 研读整本 → **故事蓝图**（人物关系、时间线、转折）。
4. **全局角色档案**：换装/年龄按场次标好；**全剧场景资产**一次铺开。
5. **分镜 = 导演指令**：按分集、场次写谁在场、镜头怎么动、切到哪。
6. **画布**：人放在场景里；三视图/光影/镜头；**截取上一镜首帧当下一镜参考**。
7. Seedance 出片：图锁脸，旧视频锁运镜，声锁节奏；局部只重做一镜。
8. 配音配乐成片在同一条链，不是另开工具。

创翊原话：旧 AI 短剧是「先画图、再生成视频」，环节割裂。小云雀是读懂剧本后自己规划分镜。

## 我们怎么对齐（目录仍用 A–F，语义换成他们的阶段）

| 小云雀 | 本仓库 | 产物 |
|---|---|---|
| 剧本 | Gate A · `01-bible/ep01.md` `STORY.md` | 能念完的故事 |
| 画风/画幅/旁白确认 | Gate A · `01-bible/confirm.md` + `LOOK.md` | 没确认不准生图 |
| 故事蓝图 | Gate A · `01-bible/blueprint.md` | 对立、转折秒、按场换装 |
| 角色/场景资产 | Gate B · `02-assets/` | master + face；场景空镜 |
| **舞台 / 画布** | **Gate S · `03-storyboard/sets.json` + 每场 `blocking.jpg`** | 人站哪，机位从哪拍 |
| **覆盖 / 运镜** | **Gate C1 · `coverage.md` + `beats.md`** | 一场几种机位，不是一人一张海报 |
| 分镜脚本 | Gate C2 · `shots.json` | 每镜是舞台上的一个 setup |
| 首帧 | Gate D · `04-frames/` | 开场从 blocking 改；续镜从上一张改 |
| 出片 | Gate E · `05-shots/` | **continue 必须 I2V 上一镜末帧** |
| 审剧情 | Gate E+ · `mix_review_track.py` | 工作旁白 + 字幕；无声切只审脸 |
| 成片 | Gate F | 高棉语轨 / 中文导出轨 |

Seedance 那种「旧视频当运镜 + 12 路混输 + 原生口型」CompShare H3 **做不到**（首帧不能混参考图）。本地能做到的导演层是：舞台、覆盖、末帧续、声画一起审。不要假装模型齐了。

## 硬规则（违反即拼接，不是导演）

1. 先有 `sets.json` 和该场 `blocking.jpg`，才允许写该场的镜头。
2. 同一场的镜头必须是**同一条轴上的覆盖**（master / close / ots / insert），禁止每人一张摆拍。
3. 场内 `cut=continue`：出视频时首帧 = 上一镜 `{id}-last.jpg`。没视频时首帧 = `image_edit` 上一镜首帧或 blocking。
4. 硬切只换场。换场下一镜从新场的 blocking 改。
5. 都市短剧每镜必有 `line` + `caption`。给你看的必须是带字带声的审片，不是无声 H3。
6. 一镜一个运动。运镜写在 `move`，整场的覆盖写在 `coverage.md`，不要每镜都 `[Push in]`。真正给模型的说明书是 `video_prompt`，对白不准写进去。

入口：`python3 scripts/check_prod.py --prod productions/<slug>`
