# 新集空壳

复制本目录，改名为 `0NN-<slug>`。顺序按 `XIAOYUNQUE.md`：`confirm.md` → 蓝图 → 主图 → `sets.json` / `render_blocking.py` → `coverage.md` → `shots.json` → `check_prod.py`。

当前关卡写在 `01-bible/STATUS.md`。未 locked 不进入下一关。高棉故事合集的画风、画幅、旁白规则以 `productions/khmer-stories/SERIES.md` 为准。
