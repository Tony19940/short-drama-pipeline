# Disabled delivery paths

`stills-to-shots.sh` is a Ken Burns still-zoom helper. It is **not** a Gate E
or Gate F delivery path. Do not write its output into `06-export/`.

Director studio and `assemble.sh` refuse Ken Burns / still-pass files as a
finished episode.
