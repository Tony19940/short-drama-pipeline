Episode- and version-specific one-shot builders.

These are not the production runtime. Call them from the compatibility
shims in `scripts/` (`python3 scripts/_build_ep01_resplit.py`, etc.) or
directly as `python3 scripts/legacy/_build_ep01_resplit.py`.
