# H3 / FL2VA

普通镜锁设计首帧再 FL2VA。同机位连续且人物有交集才吃上一镜真末帧。
要收住动作则设计尾帧且人审，路径不是 `-last.jpg`。
sheet/face 外挂锁脸；仅当服务器真有 Ref2VA 才走 Ref2VA。

派出时 `compile_h3_fields` 按官方格式写三字段，并在最前面声明图和时间轴：

- 只有首帧：`at 0.00 seconds, Picture 1 is fully referenced`
- 有设计尾帧：`Picture 1 = 0.00s`，`Picture 2 = {秒.00}s`
- 正文 `[Shot 1]`：第 0 秒站住 → 一次 body beat → 收住
- Picture 1 管构图/站位/房间/第 0 秒姿势；sheet/face 只锁身份，不替代第 0 秒
- 对白仍只在 `line` / `caption`；`non_diegetic_music: N/A`
