# 导演覆盖

先 sets.json 和 blocking.jpg。一场至少 master + 更紧机位。
新字段（新草稿必带，旧 004 缺了只警告）：story_function、end、camera_path/speed、landing、performance、sound_intent。
video_prompt 六层：主体 → 动作 → 场景 → 镜头运动 → 光影 → 风格，含起始→过程→落点。
