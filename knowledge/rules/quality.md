# 质量红线（摘要）

R1–R2 人名不译。R12 BGM 不用 H3 人声当配音。
中文对白走 Seedance 原声唇同步（台词只在 `audio_block`）；H3 / Hailuo 镜后期叠声；高棉语 Gate F。表演写行为不写情绪。首帧 = 动作起点状态，禁结果。邻镜只改一件事。默认 16:9 横幅。单集时长跟分镜走。纸面秒可低于模型下限；渲染仍受模型上下限（通常 4–15 秒）。目标 14–18 镜/分。
Ken Burns 不是成片路径。
